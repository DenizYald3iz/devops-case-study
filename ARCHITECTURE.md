# ARCHITECTURE.md
# - Genel mimari diyagramı (ASCII veya Mermaid)
# - CI/CD akış diyagramı: geliştirici → GitLab → Registry → K8s
# - Kubernetes cluster topolojisi (master + worker node'lar)
# - Monitoring veri akışı: App → Prometheus → Grafana → Alertmanager
# - Ağ katmanı: Ingress → Service → Pod akışı
